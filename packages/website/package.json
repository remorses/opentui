{
  "name": "@opentuah/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/server.tsx",
    "build": "bun build --compile --target=bun-linux-x64 src/server.tsx --outfile=dist/server-linux",
    "build:local": "bun build --compile src/server.tsx --outfile=dist/server",
    "dev:compiled": "bun run build:local && ./dist/server",
    "deploy": "bun run build && fly deploy --local-only"
  },
  "dependencies": {
    "@opentui/core": "workspace:*",
    "@opentui/react": "workspace:*",
    "@opentuah/web": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.0.0"
  }
}
